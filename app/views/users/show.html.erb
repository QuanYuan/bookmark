<p><%= @user.name %>, <%= @user.address %></p>
<%= link_to "Edit", edit_user_path(User.find(params[:id])), class: "btn btn-large btn-primary" %>
<%#= link_to "Delete", user, method: :delete,data: { confirm: "You sure?" }, class: "btn btn-large btn-primary" %>